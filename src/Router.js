import React from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';

const Router = () => {
  <BrowserRouter>
    <Routes>
      <Route />
    </Routes>
  </BrowserRouter>;
};

export default Router;
